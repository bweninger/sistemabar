springfox:
  documentation:
    swagger:
      v2:
        path: /api-docs

server:
  port: 80
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orderdb
    username: postgres
    password: password
